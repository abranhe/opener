language: c

services:
  - docker

compiler:
  - clang
  - gcc

before_install:
  - docker pull abranhe/clib
  - docker run -it -v $(pwd):/src -w /src ubuntu "ls"

script: echo hello
# docker run -it -v $(pwd):/src -w /src ubuntu sh -c "make run-test"

notifications:
  email: false